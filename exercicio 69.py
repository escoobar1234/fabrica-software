soma=0
mult=1

for c in range(0,5):
    num = float(input("digite um numero"))
    num = soma + num

print(f"A soma dos números digitados foram {soma}")
print(f"a Media dos números digitados é {(soma)/5}")